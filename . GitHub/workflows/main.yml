name: Security Analysis
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Static Analysis
        run: echo "Kod guvenlik taramasindan geciriliyor..."
